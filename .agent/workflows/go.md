---
description: Triggers Raju — shortcut for the /raju workflow
---

// turbo-all

This workflow is a shortcut for the `/raju` workflow. Run Raju:

1. Read the `/raju` workflow (`view_file`): `.agent/workflows/raju.md`

1. Apply all steps from that workflow.
